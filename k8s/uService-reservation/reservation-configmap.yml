apiVersion: v1
kind: ConfigMap
metadata:
  name: reservation-configmap
  namespace: backend
data:
  MANAGEMENT_TRACING_ENABLED: "false"
  MANAGEMENT_ZIPKIN_TRACING_ENABLED: "http://localhost:9411/api/v2/spans"
  LOGGING_CONFIG: "classpath:logback-spring-local.xml"
  service-port: "8082"
  POSTGRESQL_DB: devops_reservation_db
  SPRING_DATASOURCE_PASSWORD: cm9vdA==
  LODGE_SERVICE: "http://lodging-service:8081"
  SPRING_RABBITMQ_HOST: rabbitmq
  SPRING_RABBITMQ_PORT: "5672"
  RABBITMQ_DEFAULT_USER: guest
  RABBITMQ_DEFAULT_PASS: guest
  # LOKI_ENABLED: "false"
  # LOKI_URL: "invalid-url"
  # LOGGING_LEVEL_LOKI: "OFF"